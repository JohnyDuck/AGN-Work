# ✅ Проверка по ТЗ — ᴀᴇᴛʜᴇʀɢʟɪᴛᴄʜ TNT-Run 1.21.4

Все пункты из твоего описания перепроверены. Плагин пересобран: `AetherGlitchTNTRun-1.0.0.jar` (45KB)

### 1. База
- Paper 1.21.4, `api-version 1.21`, Java 21 — **OK** (`plugin.yml`, pom 1.21.4-R0.1)
- Готовый jar без зависимостей, кинул и работает — **OK**

### 2. Бренд
- Только `ᴀᴇᴛʜᴇʀɢʟɪᴛᴄʜ` в small caps, все остальные сообщения обычным шрифтом — **OK** (prefix `ᴀᴇᴛʜᴇʀɢʟɪᴛᴄʜ » `, messages без конвертации)

### 3. Подключение / лимиты / таймер
- `/tntrun join` / `leave` — **OK**
- `game.min-players: 2` / `max-players: 25` — **OK**
- Таймер 60 сек стартует автоматом при 2+ игроках, анонсы `60,30,20,10,5,4,3,2,1` в чат + Title + звук — **OK** (`game.countdown` + `announce-at`)
- Если игроков стало <2 — отмена — **OK**
- Начинается игра — **OK**

### 4. Плейсхолдеры
- Сделал **%agt_%** (короткий, не путается с другими играми) + legacy %aetherglitch_% для совместимости — **OK**
- Идентификатор `agt` — **OK** (проверено `javap` → `String agt`)
- Список смотри в `PLACEHOLDERS_AGT.txt` — каждый с новой строки — **OK**

### 5. Арена
- Круглая, радиус настраивается — **OK** (`radius: 20`, проверка `dx²+dz² <= r²`)
- Отдельный файл `arenas.yml` — **OK**, в `config.yml` нет секции `arena:` (проверено `grep -c arena: config.yml → 0`)
- Настраивается командами: `/tntrun setcenter` (центр+мир+floorY), `/tntrun setlobby`, `/tntrun setradius 20`, `/tntrun setfloor 80`, `/tntrun setworld world_tntrun`, `/tntrun addspawn`/`clearspawns`, `/tntrun reload` — **OK**
- Другой мир на особых координатах — **OK** (`arenas.default.world: world_tntrun`)

### 6. Механика без плит
- Падение по `PlayerMoveEvent`, без нажимных плит — **OK**
- До старта: `freeze-until-start: true` — нельзя двигаться, `no-decay-in-lobby: true` — блоки не падают — **OK**
- Во время игры: `decay-materials: [SAND,RED_SAND,GRAVEL]`, `under-material: TNT`, `decay-delay-ticks: 10` (~0.5с — не сразу и не 3 сек, как раз для паркура) — **OK**
- `instant-remove: true` — сразу в AIR, не падает до 2 этажа, `no-drops: true` — **OK**, частицы/звук — **OK**

### 7. Настройки
- `config.yml` отдельно от `arenas.yml` — **OK**
- Куча настроек: `cooldown-join-seconds`, `blocks.*`, `game.*`, `titles.*`, `sounds.*`, `messages.*`, `rewards.*`, `stats.*`, `placeholders.top-size` — **OK**

### 8. Топы / статистика / сохранения
- `stats.yml` пишет `wins/games` на каждого UUID+ник, `winrate` считается — **OK** (`StatsManager`)
- Топ по `wins` → `games`, `placeholders.top-size: 10` — **OK**
- Автосохранение каждые 60с + при победе/рестарте — **OK**

### 9. Лив / рестарт / анти-баг
- `returnLoc` сохраняет точку входа, `/tntrun leave` или `PlayerQuitEvent` — телепорт назад (`teleportAsync`), лив во время игры — элиминация, остался 1 — победа — **OK**
- `onDisable → shutdown()` возвращает онлайн игроков, `stats.save()` + `arenas.save()` — рестарт не багает — **OK**
- `resetArena()` регенит только свою арену по `original` мапе, `regenerate: true` — **OK**
- Слушатели только `if(isInGame)`, `allow-pvp: false`, `BlockBreak/Place` отменены только для участников — не ломает другие плагины — **OK**

### 10. Файлы в jar
- `config.yml` (без арены) + `arenas.yml` + `plugin.yml` + классы — **OK** (`jar tf` проверено)

Плагин готов к заливке. Если хочешь — могу ещё добавить вторую арену в `arenas.yml` пример.
